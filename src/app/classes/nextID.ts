/**
 * This represents the api getID request.
 * This class can be used for getNextPhaseID, getNextProjectID, getNextEmployeeID api calls.
 */

export class NextID {
    /** the next avaliable id from database. */
    id: number;
}